package com.algorithm.methods;

public class MinimumJumpsToReachEnd {

	
	/*  3 lists needed:-
	 *  Original-Array, Minimum-Jumps-Array, Jump-Path-Array
	 *  
	 *   1. maximum jump length = number at that index
	 *   2. write min jumps to reach at that index in Minimum-Jumps-Array
	 *   3. range of jump/legal jump 
	 *   if(i<=j+arr[j]){
	 *   	Minimum-Jumps-Array[i] = min(Minimum-Jumps-Array[i], Minimum-Jumps-Array[j]+1)
	 *   } 
	 *   
	*/

}
